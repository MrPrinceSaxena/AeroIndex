# APIx -- pinned dependencies for reproducibility
# Install: pip install -r requirements.txt
# Then: playwright install chromium
# (playwright install is a SEPARATE step -- pip install alone does NOT download browsers)

# Scraping
playwright==1.47.0

# Database
psycopg2-binary>=2.9.9
python-dotenv==1.0.1

# Data processing
pandas
numpy
matplotlib

# API
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# Utilities
python-dateutil==2.9.0
apscheduler>=3.10.0
